---
title: "Vault-LD"
description:
  "Linked Markdown vs Vault-LD: portable RDF-in-markdown specs compared."
hidden: true
---

## At a glance

|                | Linked Markdown (Wazoo)                                       | Vault-LD (The-Knowledge-Graph-Guys)                  |
| :------------- | :------------------------------------------------------------ | :--------------------------------------------------- |
| Core focus     | Formal W3C specification, multi-lang Extract API, & toolchain | Open specification & round-trip parser concept       |
| Frontmatter    | Standard JSON-LD / YAML-LD frontmatter                        | YAML-LD (JSON-LD serialized as YAML) with `@context` |
| Reference SDKs | PyPI (`linked-markdown`) & JSR (`@wazoo/linked-markdown`)     | Reference specification repo                         |
| Ecosystem      | Integrated with `wiki` CLI, Wazoo Worlds, and `memsdk`        | Standalone note vault specification                  |
| Publications   | Published W3C CG Editor's Draft & Zenodo DOI paper            | Open specification (`SPEC.md`)                       |

## The convergence of Linked Markdown and Vault-LD

Both **Linked Markdown (LMD)** (by Wazoo) and
**[Vault-LD](https://github.com/The-Knowledge-Graph-Guys/vault-ld)** (by The
Knowledge Graph Guys) arrived at the **exact same core realization**: that
standard CommonMark files with valid W3C JSON-LD / YAML-LD frontmatter form the
ideal open primitive for semantic Markdown and agent knowledge graphs.

Because both specifications converged on zero nonstandard syntax and native
JSON-LD semantics, **an LMD document and a Vault-LD document accept the exact
same document structure**. A single `.md` file functions simultaneously as
human-readable prose and a valid RDF graph node in both ecosystems.

The key distinction is not document format, but **toolchain maturity and
real-world deployment**:

- **Linked Markdown (LMD)** is backed by production-grade tooling: official
  multi-language Extract APIs (`linked-markdown-py` on PyPI,
  `@wazoo/linked-markdown` on JSR), a formal W3C Community Group Editor's Draft,
  a published research paper (Zenodo DOI: `10.5281/zenodo.21216085`), and a
  complete CLI toolchain (`wiki`, Wazoopedia, `memsdk`, and Wazoo Worlds).
- **Vault-LD** is an open specification (`SPEC.md`) emphasizing lossless
  round-trip parsing concepts for personal knowledge bases.

## Why choose Linked Markdown

- **Production-Ready SDKs**: Ready-to-install packages for Python
  (`pip install linked-markdown`) and TypeScript (`jsr:@wazoo/linked-markdown`)
  with deterministic `extract()` APIs that convert markdown directly to RDF
  quads and JSON-LD nodes.
- **W3C Community Draft & Academic Paper**: Backed by a formal W3C JSON-LD
  Community Group specification and a published DOI paper on semantic Markdown
  infrastructure.
- **End-to-End Toolchain**: Native integration with `wiki` CLI (linting, SHACL
  shape validation, SPARQL querying, and static site rendering) and Wazoo Worlds
  context engines.

## Interoperability

Because Linked Markdown and Vault-LD converged on the exact same underlying RDF
1.1 data model and JSON-LD semantics, they are **100% interoperable**. Any
Vault-LD document can be extracted, validated, and queried using
`linked-markdown` SDKs or the `wiki` CLI toolchain out of the box.
