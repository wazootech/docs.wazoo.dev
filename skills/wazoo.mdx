---
title: "Wazoo Skill"
description:
  "Give AI coding agents access to Wazoo memory with the wazoo skill."
---

```bash Quickstart
npx skills add wazootech/wazoo-skills --skill=wazoo
```

## What it does

Explains the core purpose and functionality of the tool, detailing how it breaks
down large, ambiguous efforts into a map of actionable decision tickets.

## When to reach for it

Identifies the exact scenarios and thresholds for invoking the tool,
specifically when a project is too large or "foggy" to be handled in a single
session.

## Prerequisites

Lists the required system configurations, environmental setup, or dependencies
(like issue tracker wiring) needed before the tool can operate.

## The map is an index, fog is the frontier (Concept / Mechanism Overview)

Describes the underlying conceptual model and mechanics of the tool, explaining
how it tracks known decisions as an index while treating unknown variables as a
frontier to be explored.

## It's working if

Provides concrete success criteria and expected behaviors, allowing the user to
verify that the tool is functioning and progressing correctly.

## Where it fits

Contextualizes the tool within the broader development lifecycle, explaining
what precedes its use and what downstream tools (like spec-writers or
implementation) follow it.

## Install the skill / Quickstart

Provides the exact, runnable terminal commands required to install, update, and
invoke the tool:

<CodeGroup>

```bash npm
npx skills add wazootech/wazoo-skills --skill=wazoo
```

```bash pnpm
pnpm dlx skills add wazootech/wazoo-skills --skill=wazoo
```

```bash yarn
yarn dlx skills add wazootech/wazoo-skills --skill=wazoo
```

```bash bun
bunx skills add wazootech/wazoo-skills --skill=wazoo
```

</CodeGroup>

### Update skill

<CodeGroup>

```bash npm
npx skills update wazoo
```

```bash pnpm
pnpm dlx skills update wazoo
```

```bash yarn
yarn dlx skills update wazoo
```

```bash bun
bunx skills update wazoo
```

</CodeGroup>

## Related reading

- [Agents, Skills, and MCP](/integrations/agents-skills-mcp)
- [Wazoo Platform API Reference](/platform/api)
- [Wazoo CLI Reference](/reference/cli/index)
